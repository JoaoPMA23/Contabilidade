﻿﻿﻿﻿Voc [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))   [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))  um engenhe [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) ro front-end. Gere um projeto **Next.js 14 (App Router) + Ta [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) lw [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) nd** para uma homepage  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) nst [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) tuc [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) onal de contab [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) l [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) dade com as se [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) es aba [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) xo,  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) ntegrando a um back-end v [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) a `POST /ap [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) /lead`.

**Se [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) es (ordem):**
1) Header escuro (nav: Abertura, Trocar,  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) rea do cl [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) ente, Trabalhe; CTA  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) D [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) v [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) das [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) ).
2) Hero escuro com H1  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) Procurando uma Contab [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) l [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) dade? [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))  e bullets Ef [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) caz/Qual [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) dade/Estrat [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) g [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) a (laranja).
3) Blocos:  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) Abr [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) r m [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) nha Empresa [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))  e  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) Trocar de Contador [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))  (CTAs laranja).
4) CTA v [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) nho  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) A Contab [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) l [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) dade pode te ajudar [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) .
5)  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) Nossos serv [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) os [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))  (preto,  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) cones laranja)  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))  6 cards.
6)  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) Como podemos te ajudar? [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))  WhatsApp (verde) + bot [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) o.
7)  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) Abra sua Empresa agora [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))   [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))  passos 1..4.
8)  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) Troque de Contab [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) l [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) dade [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))   [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))  passos 1..3.
9) Mapa Google Maps.
10) Contato (form  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))  `/ap [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) /lead`).
11) Footer escuro + bot [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) o flutuante WhatsApp.

**Requ [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) s [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) tos t [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) cn [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) cos:**
- TypeScr [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) pt estr [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) to; Ta [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) lw [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) nd com tema brand (dark, w [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) ne, orange, green).
- App Router; p [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) g [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) na em `app/page.tsx`.
- Formul [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) r [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) o com honeypot + fetch `${NEXT_PUBL [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) C_AP [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) _BASE_URL}/ap [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) /lead`.
- SEO b [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) s [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) co (`metadata`), `publ [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) c/robots.txt`.
- Scr [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) pts npm: dev, bu [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) ld, start.

**Etapas:**
1. `create-next-app` em TS.
2. Ta [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) lw [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) nd (postcss, conf [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) g, globals.css).
3. Tema (cores) + fontes do s [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) stema.
4.  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) mplementar se [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) es.
5. Conectar formul [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) r [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) o  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))  AP [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) .
6. Ajustar l [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) nks ( [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) rea do Cl [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) ente, WhatsApp) e embed do mapa.
7. Val [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) dar L [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
)) ghthouse ( [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(Você é um engenheiro front-end. Gere um projeto **Next.js 14 (App Router) + Tailwind** para uma homepage institucional de contabilidade com as seções abaixo, integrando a um back-end via `POST /api/lead`.

**Seções (ordem):**
1) Header escuro (nav: Abertura, Trocar, Área do cliente, Trabalhe; CTA “Dúvidas”).
2) Hero escuro com H1 “Procurando uma Contabilidade?” e bullets Eficaz/Qualidade/Estratégia (laranja).
3) Blocos: “Abrir minha Empresa” e “Trocar de Contador” (CTAs laranja).
4) CTA vinho “A Contabilidade pode te ajudar”.
5) “Nossos serviços” (preto, ícones laranja) — 6 cards.
6) “Como podemos te ajudar?” WhatsApp (verde) + botão.
7) “Abra sua Empresa agora” — passos 1..4.
8) “Troque de Contabilidade” — passos 1..3.
9) Mapa Google Maps.
10) Contato (form → `/api/lead`).
11) Footer escuro + botão flutuante WhatsApp.

**Requisitos técnicos:**
- TypeScript estrito; Tailwind com tema brand (dark, wine, orange, green).
- App Router; página em `app/page.tsx`.
- Formulário com honeypot + fetch `${NEXT_PUBLIC_API_BASE_URL}/api/lead`.
- SEO básico (`metadata`), `public/robots.txt`.
- Scripts npm: dev, build, start.

**Etapas:**
1. `create-next-app` em TS.
2. Tailwind (postcss, config, globals.css).
3. Tema (cores) + fontes do sistema.
4. Implementar seções.
5. Conectar formulário à API.
6. Ajustar links (Área do Cliente, WhatsApp) e embed do mapa.
7. Validar Lighthouse (≥ 90).
))  90).







