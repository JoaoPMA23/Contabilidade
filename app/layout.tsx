﻿﻿﻿ [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) mport type { Metadata } from "next";
 [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) mport "./globals.css";

const s [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) teName = "Spolaor Contab [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) l [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) dade";
const s [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) teUrl =
  process.env.NEXT_PUBL [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) C_S [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) TE_URL ?? "https://www.spolaorcontab [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) l [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) dade.com.br";
const metadataBase = (() => {
  try {
    return new URL(s [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) teUrl);
  } catch {
    return undef [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) ned;
  }
})();
const t [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) tle =
  "Spolaor Contab [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) l [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) dade | Consultor [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) a cont [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) b [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) l estrat [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) g [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) ca para empresas modernas";
const descr [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) pt [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) on =
  "Spolaor Contab [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) l [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) dade: abertura de empresas, m [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) gra [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
))  [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) o ass [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) st [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) da e gest [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) o f [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) scal, cont [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) b [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) l e trabalh [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) sta completa.";

export const metadata: Metadata = {
  metadataBase,
  t [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) tle: {
    default: t [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) tle,
    template: `%s | ${s [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) teName}`,
  },
  descr [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) pt [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) on,
  keywords: [
    "contab [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) l [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) dade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gest [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) o f [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) nance [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) ra",
    "BPO f [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) nance [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) ro",
  ],
  openGraph: {
    t [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) tle,
    descr [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) pt [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) on,
    url: s [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) teUrl,
    s [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) teName,
    locale: "pt_BR",
    type: "webs [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) te",
  },
  robots: {
     [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) ndex: true,
    follow: true,
  },
  alternates: {
    canon [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) cal: s [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) teUrl,
  },
};

export default funct [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) on RootLayout({
  ch [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) ldren,
}: Readonly<{
  ch [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) ldren: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="ant [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) al [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) ased">{ch [Text.Encoding]::UTF8.GetString([Text.Encoding]::Default.GetBytes(import type { Metadata } from "next";
import "./globals.css";

const siteName = "Spolaor Contabilidade";
const siteUrl =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://www.spolaorcontabilidade.com.br";
const metadataBase = (() => {
  try {
    return new URL(siteUrl);
  } catch {
    return undefined;
  }
})();
const title =
  "Spolaor Contabilidade | Consultoria contábil estratégica para empresas modernas";
const description =
  "Spolaor Contabilidade: abertura de empresas, migração assistida e gestão fiscal, contábil e trabalhista completa.";

export const metadata: Metadata = {
  metadataBase,
  title: {
    default: title,
    template: `%s | ${siteName}`,
  },
  description,
  keywords: [
    "contabilidade",
    "contador",
    "abertura de empresa",
    "trocar de contador",
    "gestão financeira",
    "BPO financeiro",
  ],
  openGraph: {
    title,
    description,
    url: siteUrl,
    siteName,
    locale: "pt_BR",
    type: "website",
  },
  robots: {
    index: true,
    follow: true,
  },
  alternates: {
    canonical: siteUrl,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className="antialiased">{children}</body>
    </html>
  );
}
)) ldren}</body>
    </html>
  );
}






